<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.demo"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="23" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".provider.MainActivity"
            android:label="@string/app_name" >
        </activity>

        <receiver
            android:name="com.example.demo.provider.StartServiceWidgetProvider"
            android:exported="true"
            android:label="@string/app_name1"
            android:process=":remote" >
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@layout/widget_info" />
        </receiver>

        <service
            android:name="com.example.demo.service.GalleryService"
            android:process=":remote" >
        </service>

        <receiver
            android:name="com.example.demo.provider.AddSelfActionWidgetProvider"
            android:exported="true"
            android:label="@string/app_name"
            android:process=":remote" >
            <intent-filter>

                <!-- widget默认的事件action -->
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <!-- 自己加的 点击小部件响应的事件 -->
                <action android:name="com.example.demo.provider.click_widget.layout" />
            </intent-filter>
            <!-- widget元数据,name是写死的，resource指的是widget的配置文件 -->
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@layout/widget_info" />
        </receiver>
        
        <receiver
            android:name="com.example.demo.provider.ClockWidgetProvider"
            android:exported="true"
            android:label="@string/app_name_clock"
            android:process=":remote" >
            <intent-filter>

                <!-- widget默认的事件action -->
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <!-- 自己加的 点击小部件响应的事件 -->
                <action android:name="com.example.demo.provider.click_widget.layout" />
            </intent-filter>
            <!-- widget元数据,name是写死的，resource指的是widget的配置文件 -->
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@layout/widget_clock_info" />
        </receiver>
    </application>

</manifest>