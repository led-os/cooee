<resources>

    <!-- 在manifest.xml中配置android:largeHeap="@bool/config_largeHeap"，设为true时可以增加内存的申请量 start -->
    <bool name="config_largeHeap">false</bool>
    <!-- 在manifest.xml中配置android:largeHeap="@bool/config_largeHeap"，设为true时可以增加内存的申请量 end -->


    <!-- Max number of page indicators to show -->
    <integer name="config_maxNumberOfPageIndicatorsToShow">16</integer>

    <!-- DragController -->
    <integer name="config_flingToDeleteMinVelocity">-1500</integer>
    <integer name="config_animation_duration_when_workspace_to_applist">300</integer>
    <integer name="config_animation_duration_when_workspace_to_editmode">250</integer>
    <integer name="config_animation_duration_when_editmode_to_workspace">250</integer>

    <!-- Out of 100, the percent to shrink the workspace during spring loaded mode. -->
    <integer name="config_workspaceSpringLoadShrinkPercentage">80</integer>
    <!-- Out of 100, the percent to shrink the workspace during overview mode. -->
    <integer name="config_workspaceOverviewShrinkPercentage">58</integer>

    <!-- Fade/zoom in/out duration & scale in the AllApps transition. -->
    <!-- Note: This should be less than the workspaceShrinkTime as they happen together. -->
    <integer name="config_animation_duration_when_applist_to_workspace">300</integer>
    <integer name="config_appsCustomizeZoomInTime">350</integer>
    <integer name="config_appsCustomizeZoomOutTime">600</integer>
    <integer name="config_appsCustomizeZoomScaleFactor">7</integer>
    <integer name="config_appsCustomizeFadeInTime">250</integer>
    <integer name="config_appsCustomizeFadeOutTime">200</integer>
    <integer name="config_appsCustomizeWorkspaceAnimationStagger">40</integer>
    <integer name="config_workspaceAppsCustomizeAnimationStagger">100</integer>

    <!-- Tab transition animation duration -->
    <integer name="config_tabTransitionDuration">250</integer>

    <!-- The slope, in percent, of the drag movement needed to drag an item out of -->
    <!-- AppsCustomize (y / x * 100%) -->
    <integer name="config_appsCustomizeDragSlopeThreshold">150</integer>

    <!-- Workspace -->
    <!-- Whether or not the drop targets drop down as opposed to fade in -->
    <!-- 设为true时，删除框、搜索框出现或消失的动画是平移，位置移动，当设为false时，动画是透明度变化 -->
    <bool name="config_useDropTargetDownTransition">false</bool>
    <!-- Whether or not to fade the side pages -->
    <!-- 是否在滑页时临近页变透，目前代码中起保护作用 -->
    <bool name="config_workspaceFadeAdjacentScreens">false</bool>

    <!-- The transition duration for the background of the drop targets -->
    <integer name="config_dropTargetBgTransitionDuration">0</integer>

    <!-- The duration (in ms) of the fade animation on the object outlines, used when -->
    <!-- we are dragging objects around on the home screen. -->
    <integer name="config_dragOutlineFadeTime">900</integer>

    <!-- The alpha value at which to show the most recent drop visualization outline. -->
    <integer name="config_dragOutlineMaxAlpha">128</integer>

    <!-- Parameters controlling the animation for when an item is dropped on the home screen, -->
    <!-- and it animates from its old position to the new one. -->
    <integer name="config_dropAnimMinDuration">100</integer>
    <integer name="config_dropAnimMaxDuration">500</integer>

    <!-- The duration of the UserFolder opening and closing animation -->
    <integer name="config_folderAnimDuration">120</integer>

    <!-- The distance at which the animation should take the max duration -->
    <integer name="config_dropAnimMaxDist">800</integer>

    <!-- Camera distance for the overscroll effect -->
    <integer name="config_cameraDistance">8000</integer>

    <!-- Memory debugging, including a memory dump icon -->
    <!-- 内存分析，设为true，在其他桌面会多出现两个icon（我们桌面屏蔽掉了），点击*HPROF,会在SD卡中生成文件，可进行内存分析 -->
    <bool name="debug_memory_enabled">false</bool>

    <integer name="config_editmode_secondary_duration">150</integer>

    <!-- //添加配置项“switch_enable_customer_lxt_change_custom_config_path”，是否支持客户“凌星通”定制的“切换本地化配置文件的文件夹”功能。true为支持，false为不支持。默认为false。	//xiatian add start -->
    <!-- 【备注】 -->
    <!-- 1、“launcher_default_config.xml”文件、“default_workspace.xml”文件和“default_workspace_double.xml”文件的默认本地化文件为“system/launcher” -->
    <!-- 2、有两种不同的模式：“读取广播”模式和“读取nv映射表”模式。默认为“读取广播”模式。 -->
    <!-- 3、“读取广播”模式逻辑如下 -->
    <!-- 3.1、收到广播时（action值为cooee_phenix_customer_lxt_change_custom_config_path_style_broadcast_action；附加参数为：（1）类型：String；（2）关键字：cooee_phenix_customer_lxt_change_custom_config_path_style_broadcast_path；（3）关键字对应的内容：本地化配置文件的文件夹的全路径（例如：/system/launcher）。） -->
    <!-- 3.1.1、将广播中的“本地化配置文件的文件夹”路径，写入T卡 -->
    <!-- 3.1.2、清空数据 -->
    <!-- 3.1.3、重启桌面 -->
    <!-- 3.2、每次启动桌面时 -->
    <!-- 3.2.1、桌面数据库中的“本地化配置文件的文件夹”路径 -->
    <!-- 3.2.2、上述步骤读不到路径，则读取T卡存的路径，并写入桌面数据库（防止T卡被移除，数据双备份） -->
    <!-- 3.2.3、上述步骤读不到路径，则使用默认路径（system/launcher） -->
    <!-- 4、“读取nv映射表”模式逻辑如下 -->
    <!-- 4.1、收到广播时 -->
    <!-- 4.1.1、清空数据 -->
    <!-- 4.1.2、重启桌面 -->
    <!-- 4.2、每次启动桌面时 -->
    <!-- 4.2.1、读nv映射表（“负数N”或者“0”对应/system/launcher，“正数M”对应的是/system/launcher“M+1”）对应的“本地化配置文件的文件夹”路径 -->
    <!-- 4.2.2、上述步骤读不到路径，则使用默认路径（system/launcher） -->
    <bool name="switch_enable_customer_lxt_change_custom_config_path">false</bool>
    <!-- //添加配置项“switch_enable_customer_lxt_change_custom_config_path”，是否支持客户“凌星通”定制的“切换本地化配置文件的文件夹”功能。true为支持，false为不支持。默认为false。	//xiatian add end -->


    <!-- //zhujieping add start //拓展配置项“config_applist_in_and_out_anim_style”，添加可配置项2。2是仿S8进入主菜单的动画。 -->
    <dimen name="applist_in_and_out_anim_s8_style_move_up_offset">50dp</dimen>
    <!-- //zhujieping add end //拓展配置项“config_applist_in_and_out_anim_style”，添加可配置项2。2是仿S8进入主菜单的动画。 -->

</resources>